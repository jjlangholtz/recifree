<li>
  <div><%= link_to image_tag(recipe.photo.thumb.url), recipe_path(recipe) %></div>
  <div><%= link_to recipe.name, recipe_path(recipe) %></div>
  <div>
    <% recipe.tags.each do |tag| %>
      <span class="secondary radius label"><%= link_to tag, tag_recipes_path(tag.name) %></span>
    <% end %>
  </div>
</li>
